# Sample Site

Sitio minimo para validar `osg build` y `osg serve` sin depender de un vault real.

## Uso
```bash
cd examples/sample-site
osg build
osg serve
```

El config apunta al theme default del repo (`../../themes`).
